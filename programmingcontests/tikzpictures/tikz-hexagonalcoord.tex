\begin{tikzpicture}
\begin{scope}[y={(0.5 cm,0.866 cm)}]
\foreach \x in {1,...,5} {
  \foreach \y in {1,...,5} {
    \ifthenelse{\not\equal{\intcalcMod{\x+2*\y}{3}}{0}} {
    \fill (\x,\y) circle (1 mm);
    \begin{scope}[thick,blue]
    \ifthenelse{\equal{\intcalcMod{\x+2*\y}{3}}{1}} {
      \draw (\x+1,\y) -- (\x,\y) -- (\x,\y-1) (\x-1,\y+1) -- (\x,\y);
    }{
      \draw (\x-1,\y) -- (\x,\y) -- (\x,\y+1) (\x+1,\y-1) -- (\x,\y);
    }
    \end{scope}
    }{
    \begin{scope}[red]\fill (\x,\y) circle (0.5 mm);\draw (\x-1,\y) -- (\x,\y) -- (\x+1,\y) (\x,\y-1) -- (\x,\y) -- (\x,\y+1) (\x+1,\y-1) -- (\x,\y) -- (\x-1,\y+1);\end{scope}
    }
  }
}
\end{scope}
\end{tikzpicture}